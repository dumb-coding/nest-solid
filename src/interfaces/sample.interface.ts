/**
 * Sample entity shape shared across the sample feature.
 */
export interface SampleEntity {
  /** Unique identifier for the sample entity. */
  id: string;
  /** The display title for the sample entity. */
  title: string;
}
